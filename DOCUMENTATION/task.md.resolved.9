# Omnidex dApp — Task Tracker

## Initial Build
- [x] **Scaffold** — Next.js + Tailwind + wagmi + viem
- [x] **Core Components** — SwapBridgeApp, ChainSelector, TokenSelector, AmountInput, QuotePanel, TransactionStepper
- [x] **Provider Adapters** — 1inch, Jupiter, LI.FI, deBridge (mock mode)

## Logo & Wallet Fixes
- [x] **Chain Logos** — Downloaded and verified all 35 chain logos, fixed 8 incorrect ones
- [x] **Wallet Connectors** — Replaced custom wallet UI with RainbowKit (19 wallets with official logos)

## OTC / P2P Trading
- [x] **Types & Store** — OTC order types, localStorage persistence
- [x] **useOTCOrders Hook** — Create, fill, cancel orders with mock escrow
- [x] **OTC Components** — OTCApp, CreateOrder, OrderBook, OrderCard, FillOrderModal
- [x] **Navigation** — Tab nav on page.tsx to switch between Swap & OTC
- [x] **Compile Verified** — Dev server returns 200 OK
